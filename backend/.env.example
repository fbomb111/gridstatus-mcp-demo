# Required: Microsoft Foundry endpoint for AI features
FOUNDRY_ENDPOINT=https://your-resource.openai.azure.com/openai/v1/

# Optional: gridstatus.io API key (unlocks historical data tool)
# GRIDSTATUS_API_KEY=your-key-here

# Required: Managed Identity client ID for Azure auth
MANAGED_IDENTITY_CLIENT_ID=your-client-id-here

# Optional: Model deployment name (default: gpt-4.1)
FOUNDRY_MODEL_DEPLOYMENT=gpt-4.1

# Optional: Allowed CORS origins (default: localhost only)
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# Optional: Environment name (local, production)
ENVIRONMENT=local

# Optional: Set by CI/CD for deployment tracking
# GIT_SHA=abc123
